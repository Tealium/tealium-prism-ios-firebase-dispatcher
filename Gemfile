source "https://rubygems.org"

gem "fastlane"
gem "cocoapods"
gem "jazzy"
# Pin below 1.1.2: that release breaks `Mustache.template_path=` on Ruby 3.3+
# (`Pathname#map` doesn't exist) — jazzy's default theme setup crashes otherwise.
gem "mustache", "1.1.1"
